<h1>Tasks</h1>

<ul>
<% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task.id) %> - <%= link_to "Edit", edit_task_path(task.id) %></li>
<% end %>
</ul>


<%= link_to "New Task", new_task_path %>
